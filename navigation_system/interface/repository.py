from abc import ABC, abstractmethod


class Repository(ABC):
    """
    Base para todos los repositorios que manejan la persistencia de datos. Todas las clases que representarán un repositorio deben heredar de esta clase.
    """
    pass
